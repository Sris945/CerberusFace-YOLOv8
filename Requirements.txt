# Core YOLOv8 dependencies
ultralytics>=8.0.0

# PyTorch (choose CUDA version if you want GPU, otherwise CPU)
torch>=2.0.0
torchvision>=0.15.0

# Data handling & preprocessing
opencv-python
numpy
pandas
Pillow

# Visualization
matplotlib
seaborn

# Utilities
tqdm
pyyaml
scipy

# Optional: Export & Deployment
onnx
onnxruntime

#Notes:
        # If you have a GPU with CUDA, you should install the right Torch + CUDA version manually (because pip might default to CPU). Example:
        # pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 -f https://download.pytorch.org/whl/torch_stable.html
